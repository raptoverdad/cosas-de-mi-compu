const express = require("express")
const path =require('path') //working with routes in node
const app=express()
//starting server
app.listen(3000,()=>{
    console.log('server up at port 3000')
})
console.log() //join une direcciones
//static files


